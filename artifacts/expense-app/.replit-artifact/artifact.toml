kind = "web"
previewPath = "/"
title = "Smart Expense & Budget Manager"
version = "1.0.0"
id = "artifacts/expense-app"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 3000

[services.development]
run = "pnpm --filter @workspace/expense-app run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/expense-app", "run", "build" ]
publicDir = "artifacts/expense-app/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "3000"
BASE_PATH = "/"
